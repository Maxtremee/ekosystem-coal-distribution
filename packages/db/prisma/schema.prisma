generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Invoice {
  id                 String       @id @default(cuid())
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt
  createdBy          String //email
  updatedBy          String? //email
  name               String
  issueDate          DateTime
  applicationId      String
  declaredNutCoal    Decimal?
  declaredEcoPeaCoal Decimal?
  nutCoalLeft        Decimal?
  EcoPeaCoalLeft     Decimal?
  stockIssues        StockIssue[]
}

model DistributionCenter {
  id         String       @id @default(cuid())
  name       String
  address    String       @db.Text
  email      String
  StockIssue StockIssue[]
}

model StockIssue {
  id               String             @id @default(cuid())
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  issuedBy         DistributionCenter @relation(fields: [distributionCenterId], references: [id])
  nutCoalIssued    Decimal?
  ecoPeaCoalIssued Decimal?

  Invoice              Invoice? @relation(fields: [invoiceId], references: [id])
  invoiceId            String?
  distributionCenterId String

  @@index([invoiceId])
  @@index([distributionCenterId])
}
